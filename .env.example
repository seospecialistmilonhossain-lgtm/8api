# Environment Configuration Example
# Copy this to .env and update values (optional - app works without .env)

# Application
APP_NAME="Advanced Scraper API"
APP_VERSION="2.0.0"
DEBUG=false

# Security (CHANGE THESE IN PRODUCTION!)
SECRET_KEY="change-this-to-a-secure-random-string-min-32-characters-long"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS - Update with your frontend domains
CORS_ORIGINS=["*"]
CORS_ALLOW_CREDENTIALS=true

# Database
# SQLite (default)
DATABASE_URL="sqlite+aiosqlite:///./scraper.db"
# PostgreSQL (uncomment and update for production)
# DATABASE_URL="postgresql+asyncpg://username:password@localhost:5432/scraper_db"

# Rate Limiting (in-memory)
RATE_LIMIT_ENABLED=true
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000

# Logging
LOG_LEVEL="INFO"
LOG_FORMAT="json"

# Compression
ENABLE_GZIP=true
GZIP_MIN_SIZE=1024

# HLS Proxy
HLS_PROXY_ENABLED=true
HLS_PROXY_TIMEOUT=30

# Static/CDN
# Leave empty to use local /static from FastAPI.
STATIC_CDN_BASE_URL=""
# Default cache for non-fingerprinted static files.
STATIC_CACHE_MAX_AGE=3600
# Cache for fingerprinted immutable assets.
STATIC_IMMUTABLE_MAX_AGE=31536000
# Comma-separated regex list; files matching any pattern are treated immutable.
STATIC_IMMUTABLE_PATTERNS="\\.[a-f0-9]{8,}\\."

# Scraping
SCRAPER_TIMEOUT=30
SCRAPER_MAX_RETRIES=3
SCRAPER_RETRY_DELAY=2

# Authentication
REQUIRE_AUTH=false
API_KEY_HEADER="X-API-Key"
